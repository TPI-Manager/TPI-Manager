# FRONTEND CONFIGURATION (Vite)
# -----------------------------
# Firebase Client SDK Keys
# Get these from Firebase Console -> Project Settings -> General -> Your Apps -> Web App -> Config
VITE_FIREBASE_API_KEY=AIzA...
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef

# Backend API URL
# Local: http://localhost:5000
# Production: leave empty to use relative path (if served by same origin) or set full URL
# But since we use conditional config in config.js based on import.meta.env.PROD, 
# you might not strictly need this if config.js handles it. 
# However, if you want to override:
# VITE_API_BASE=http://localhost:5000

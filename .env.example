# BACKEND CONFIGURATION
# ---------------------
# Port for the server
PORT=5000

# Environment (development or production)
NODE_ENV=development

# Frontend URL for CORS (Allow requests from this URL)
# In local dev: http://localhost:5173
# In production: https://your-vercel-app.app
CLIENT_URL=http://localhost:5173

# Firebase Admin SDK Service Account
# Option A (Recommended for Vercel): Content of serviceAccountKey.json as a single line string
FIREBASE_SERVICE_ACCOUNT=

# Option B (Local Dev): Path to the file is handled automatically if 'serviceAccountKey.json' exists in root.
# So locally you can leave the variable above empty if you have the file.

# Firebase Storage Bucket (Optional)
# Format: your-project.appspot.com
FIREBASE_STORAGE_BUCKET=

# Initial Password for the Default Admin User (auto-seeded)
ADMIN_INIT_PASS=admin123
